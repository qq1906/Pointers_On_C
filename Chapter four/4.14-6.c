/**
 * @file 4.14-6.c
 * @author Power (q1225409583@gmail.com)
 * @brief 
 * @version 0.1
 * @date 2022-04-06
 * 
 * @copyright Copyright (c) 2022
 * 
 * 编写一个函数，它从一个字符串中提取一个子字符串。函数的原型如下：
 * int substr(char dest[], char src[], int start, int len);
 * 函数的任务是从src数组起始位置向后偏移start个字符的位置开始，
 * 最多复制len个非NUL字符到dest数组。在复制完毕后，dest数组必须
 * 以NUL字节结尾。函数的返回值是存储与dest数组中的字符串的长度。
 * 
 * 如果start所指定的位置越过了src数组的尾部，或者start或len的值为负，
 * 把呢复制到dest数组的是一个空字符串。
 */
#include <stdio.h>

int substr(char dest[], char src[], int start, int len)
{
    
}

int main()
{
   
   return 0;
}
